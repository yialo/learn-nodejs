extends _layout

block title
  = 'Chat | '

block content
  include ./_nav.pug
  main
    h1 Чат
    p= user ? `Пользователь: ${user.get('username')}` : 'Вы не авторизованы'
  script(src='/socket.io/socket.io.js')
  script.
    (() => {
      const socket = io.connect();
      socket.on('news', (data) => {
        console.log(data);
      });
      socket.emit('another', { some: 'bob' });
    })();
